@using Service.ViewModels.Setting

@model List<SettingListVM>

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <a class="btn btn-success" asp-area="Admin" asp-controller="Setting" asp-action="Create">Create Setting</a>


            <table class="table table-striped">
                <thead>
                    <tr>
                        <th> Key </th>
                        <th> Value  </th>
                        <th> Settings </th>
                    </tr>
                </thead>
                <tbody>

                    @foreach (var item in Model)
                    {
                        <tr>

                            <td> @item.Key </td>
                            <td> @item.Value </td>
                            <td>
                                <a class="btn btn-info" asp-area="Admin" asp-controller="Setting" asp-route-id="@item.Id" asp-action="Detail">Detail</a>
                                <a class="btn btn-primary" asp-area="Admin" asp-controller="Setting" asp-route-id="@item.Id" asp-action="Update">Update</a>
                                <a class="btn btn-danger" asp-area="Admin" asp-controller="Setting" asp-route-id="@item.Id" asp-action="Delete">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>